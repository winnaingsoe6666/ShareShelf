---
phase: 01-fix-critical-issues-testing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/components/ui/__tests__/Button.test.tsx
  - frontend/src/components/ui/__tests__/Input.test.tsx
  - frontend/src/components/ui/__tests__/Card.test.tsx
  - frontend/src/components/ui/__tests__/Badge.test.tsx
  - frontend/src/components/ui/__tests__/Modal.test.tsx
  - frontend/src/components/ui/__tests__/Spinner.test.tsx
  - frontend/src/components/layout/__tests__/Navbar.test.tsx
  - frontend/src/lib/__tests__/utils.test.ts
  - frontend/src/lib/__tests__/auth.test.ts
  - frontend/src/lib/__tests__/api.test.ts
autonomous: true
requirements:
  - TEST-02
user_setup:
  - service: npm
    why: "Install Vitest and React Testing Library dependencies for frontend testing"
    env_vars: []
must_haves:
  truths:
    - "Developer can run `npx vitest run` and all UI component tests pass"
    - "Developer can run `npx vitest run` and all utility tests pass"
    - "Test framework is configured with jsdom environment and @/ path alias"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vitest + RTL dev dependencies"
      contains: "vitest"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment and @/ alias"
      min_lines: 12
    - path: "frontend/src/test/setup.ts"
      provides: "Test setup importing @testing-library/jest-dom"
      min_lines: 1
    - path: "frontend/src/components/ui/__tests__/Button.test.tsx"
      provides: "Button component tests covering all variants (primary, secondary, outline, ghost, danger), sizes (sm, md, lg), loading state, and disabled state"
    - path: "frontend/src/components/ui/__tests__/Input.test.tsx"
      provides: "Input component tests covering render, label, error state, disabled state, and ref forwarding"
    - path: "frontend/src/components/ui/__tests__/Card.test.tsx"
      provides: "Card component tests covering default render, clickable variant, and non-clickable variant"
    - path: "frontend/src/components/ui/__tests__/Badge.test.tsx"
      provides: "Badge component tests covering all variants (default, success, warning, danger, info)"
    - path: "frontend/src/components/ui/__tests__/Modal.test.tsx"
      provides: "Modal component tests covering open state, closed state, escape key close, overlay click close, and title render"
    - path: "frontend/src/components/ui/__tests__/Spinner.test.tsx"
      provides: "Spinner component tests covering default render, className passthrough, and no-text output"
    - path: "frontend/src/components/layout/__tests__/Navbar.test.tsx"
      provides: "Navbar component tests covering logged-in state, logged-out state, mobile menu toggle"
    - path: "frontend/src/lib/__tests__/utils.test.ts"
      provides: "Utility function tests: formatPrice (with value, null, undefined), formatDate (with date, empty/undefined), cn (class joining)"
    - path: "frontend/src/lib/__tests__/auth.test.ts"
      provides: "Auth lib tests: saveAuth stores token+user, getToken retrieves token, getUser parses user, clearAuth removes both, isAuthenticated returns boolean"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*setup"
    - from: "frontend/vitest.config.ts"
      to: "frontend/tsconfig.json"
      via: "resolve.alias @/"
      pattern: "alias.*@"
    - from: "frontend/src/components/ui/__tests__/*.test.tsx"
      to: "frontend/src/components/ui/*.tsx"
      via: "import"
      pattern: "import.*from.*\\.\\./"
---

<objective>
Set up Vitest + React Testing Library for the frontend and write comprehensive unit tests for all UI components (Button, Input, Card, Badge, Modal, Spinner, Navbar) and all lib utilities (utils, auth, api).

Purpose: Establish frontend testing infrastructure per D-04/D-05 so that Phase 1 TDD cycles can write failing tests before frontend fixes. Component and utility tests should pass on first run since these files have no bugs.
Output: Working `npx vitest run` with 12+ test files passing, covering all UI component states and lib functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@file:.planning/phases/01-fix-critical-issues-testing-foundation/01-CONTEXT.md
@file:.planning/phases/01-fix-critical-issues-testing-foundation/01-UI-SPEC.md
@file:.planning/codebase/CONVENTIONS.md
@file:.planning/codebase/TESTING.md

<interfaces>
From frontend/src/components/ui/Button.tsx:
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: "primary" | "secondary" | "outline" | "ghost" | "danger";
  size?: "sm" | "md" | "lg";
  loading?: boolean;
}
// Default exports a function component
```

From frontend/src/components/ui/Input.tsx:
```typescript
interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label?: string;
  error?: string;
}
// Uses forwardRef<HTMLInputElement, InputProps>
// Sets Input.displayName = "Input"
```

From frontend/src/components/ui/Card.tsx:
```typescript
interface CardProps {
  children: React.ReactNode;
  className?: string;
  onClick?: () => void;
}
```

From frontend/src/components/ui/Badge.tsx:
```typescript
interface BadgeProps {
  children: React.ReactNode;
  variant?: "success" | "warning" | "danger" | "info" | "default";
  className?: string;
}
```

From frontend/src/components/ui/Modal.tsx:
```typescript
interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
}
```

From frontend/src/components/ui/Spinner.tsx:
```typescript
interface SpinnerProps {
  className?: string;
}
```

From frontend/src/components/layout/Navbar.tsx:
```typescript
// No explicit props interface — reads auth state internally via isAuthenticated() + getUser()
// Has mobile menu toggle via local useState
```

From frontend/src/lib/utils.ts:
```typescript
export function formatDate(dateString?: string): string;
export function formatPrice(amount?: number): string;
export function cn(...classes: (string | boolean | undefined | null)[]): string;
```

From frontend/src/lib/auth.ts:
```typescript
export function saveAuth(auth: AuthResponse): void;
export function clearAuth(): void;
export function getToken(): string | null;
export function getUser(): User | null;
export function isAuthenticated(): boolean;
```
</interfaces>

<testing_requirements>
Per UI-SPEC.md component state checklist (see source) and TESTING.md recommended patterns:
- Each component test MUST verify: default render, variant/state CSS classes, event handlers, conditional rendering
- Mock pattern: vi.mock for next/navigation useRouter, vi.mock for lib/auth, no mock for simple components
- Use describe/it blocks with vi.fn() for handlers
- Assert CSS classes with toHaveClass, text content with toBeInTheDocument/toHaveTextContent
</testing_requirements>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + RTL and create test configuration files</name>

  <files>
    frontend/package.json
    frontend/vitest.config.ts
    frontend/src/test/setup.ts
  </files>

  <read_first>
    frontend/package.json — current dependencies (no test deps), scripts (no test script)
    frontend/tsconfig.json — path alias @/ -> ./src/*
    frontend/next.config.ts — existing config for reference
  </read_first>

  <action>
    Install Vitest and React Testing Library dependencies into the frontend project using npm:

    Required packages (add to devDependencies):
    - vitest: ^3.0.0
    - @testing-library/react: ^16.2.0
    - @testing-library/jest-dom: ^6.6.0
    - @testing-library/user-event: ^14.6.0
    - jsdom: ^25.0.0 or ^26.0.0 (compatible with Node 18+)

    Add three npm scripts to package.json:
    - "test": "vitest run"
    - "test:watch": "vitest"
    - "test:coverage": "vitest run --coverage"

    Create vitest.config.ts at frontend/ root:
    - Use defineConfig from vitest/config
    - test.environment: "jsdom"
    - test.globals: true
    - test.setupFiles: "./src/test/setup.ts"
    - resolve.alias: { "@": path.resolve(__dirname, "./src") } matching tsconfig paths

    Create frontend/src/test/setup.ts:
    - Single import: `import "@testing-library/jest-dom/vitest";` (vitest-compatible import)
    - If that exact import path fails at runtime, fall back to: `import "@testing-library/jest-dom";` and add `/// <reference types="@testing-library/jest-dom" />` to a `.d.ts` file or vitest config.

    Run `npm install` after modifying package.json. Then run `npx vitest run` to verify the config loads (will show "no test files" which is expected at this stage — accept that output as success).
  </action>

  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `frontend/node_modules/vitest` exists after install
    - `frontend/node_modules/@testing-library/react` exists
    - `frontend/node_modules/jsdom` exists
    - `frontend/vitest.config.ts` contains `environment: "jsdom"`, `globals: true`, and `setupFiles`
    - `frontend/vitest.config.ts` contains resolve alias for `@` pointing to `./src`
    - `frontend/src/test/setup.ts` exists and imports @testing-library/jest-dom
    - `npx vitest run` exits with status 0 (even if zero test files — config has loaded)
    - `package.json` contains scripts: "test", "test:watch", "test:coverage"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write UI component tests for Button, Input, Card, Badge, Modal, Spinner</name>

  <files>
    frontend/src/components/ui/__tests__/Button.test.tsx
    frontend/src/components/ui/__tests__/Input.test.tsx
    frontend/src/components/ui/__tests__/Card.test.tsx
    frontend/src/components/ui/__tests__/Badge.test.tsx
    frontend/src/components/ui/__tests__/Modal.test.tsx
    frontend/src/components/ui/__tests__/Spinner.test.tsx
  </files>

  <read_first>
    frontend/src/components/ui/Button.tsx — extract variant class mapping (primary=bg-emerald-600, secondary=bg-stone-800, outline=border-stone-300, ghost=text-stone-600, danger=bg-red-600), size classes (sm=px-3, md=px-4, lg=px-6)
    frontend/src/components/ui/Input.tsx — extract label/error/ref patterns
    frontend/src/components/ui/Card.tsx — extract clickable vs non-clickable class patterns
    frontend/src/components/ui/Badge.tsx — extract variant class mapping (success=bg-emerald-100, warning=bg-amber-100, danger=bg-red-100, info=bg-blue-100, default=bg-stone-100)
    frontend/src/components/ui/Modal.tsx — extract isOpen conditional rendering, escape/overlay handlers
    frontend/src/components/ui/Spinner.tsx — extract SVG animation pattern (animate-spin class)
    01-UI-SPEC.md component state checklist — authoritative source for every state to test
  </read_first>

  <action>
    Create `__tests__/` directory under `frontend/src/components/ui/` and write six test files. Follow the component state checklist from 01-UI-SPEC.md exactly:

    Button.test.tsx:
    - Test render of children text
    - Test onClick fires via fireEvent.click
    - Test loading state: button is disabled AND contains an SVG spinner
    - Test each variant (primary, secondary, outline, ghost, danger) applies correct class: bg-emerald-600, bg-stone-800, border-stone-300, text-stone-600, bg-red-600
    - Test each size (sm, md, lg) applies px class: px-3, px-4, px-6
    - Test disabled attribute present when disabled prop is set

    Input.test.tsx:
    - Test renders input element
    - Test label text rendered when label prop provided
    - Test error message rendered in red-700 text when error prop provided
    - Test disabled attribute passes through
    - Test forwardRef behavior (pass ref and check input element matches)

    Card.test.tsx:
    - Test default render: children visible, rounded-xl class
    - Test clickable: onClick prop adds cursor-pointer class
    - Test non-clickable: no cursor-pointer class

    Badge.test.tsx:
    - Test renders children text
    - Test each variant class: success=bg-emerald-100, warning=bg-amber-100, danger=bg-red-100, info=bg-blue-100, default=bg-stone-100

    Modal.test.tsx:
    - Test isOpen=true renders dialog with backdrop
    - Test isOpen=false renders nothing (container not in DOM)
    - Test Escape key calls onClose (fireEvent.keyDown on document)
    - Test overlay click calls onClose
    - Test title text rendered when title prop provided

    Spinner.test.tsx:
    - Test SVG rendered with animate-spin class
    - Test custom className passed to container
    - Test no text rendered (only SVG children)

    All tests must use `import { describe, it, expect, vi } from "vitest"` and `import { render, screen, fireEvent } from "@testing-library/react"`.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/__tests__/ 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - 6 test files exist under frontend/src/components/ui/__tests__/
    - `npx vitest run` passes all 6 component test files
    - Button tests cover all 5 variants (class assertions), 3 sizes, loading+disabled states
    - Input tests cover render, label, error, disabled, forwardRef
    - Card tests cover default and clickable variants
    - Badge tests cover all 5 variant classes
    - Modal tests cover open, closed, escape key, overlay click
    - Spinner tests cover SVG render, className passthrough, no text
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write Navbar component tests and lib utility/auth tests</name>

  <files>
    frontend/src/components/layout/__tests__/Navbar.test.tsx
    frontend/src/lib/__tests__/utils.test.ts
    frontend/src/lib/__tests__/auth.test.ts
    frontend/src/lib/__tests__/api.test.ts
  </files>

  <read_first>
    frontend/src/components/layout/Navbar.tsx — reads isAuthenticated/getUser from lib/auth, has mobile menu toggle via useState, shows different links based on auth state
    frontend/src/lib/utils.ts — formatDate, formatPrice, cn functions
    frontend/src/lib/auth.ts — saveAuth, clearAuth, getToken, getUser, isAuthenticated using localStorage keys "shareshelf_token" and "shareshelf_user"
    frontend/src/lib/api.ts — Axios instance with interceptors, uses NEXT_PUBLIC_API_URL env var, fallback "/api"
  </read_first>

  <action>
    Create `__tests__/` directory under `frontend/src/components/layout/` and `frontend/src/lib/`. Write four test files:

    Navbar.test.tsx:
    - Mock `@/lib/auth` with vi.mock: isAuthenticated returns true/false, getUser returns { id: 1, name: "Test User", email: "test@test.com" }
    - Mock `next/navigation` useRouter: returns { push: vi.fn() }
    - Test logged-in state: Browse, Add Item, My Borrows, Profile, Log Out links visible; Log In and Sign Up links NOT visible
    - Test logged-out state: Browse, Log In, Sign Up links visible; My Borrows, Profile, Log Out NOT visible
    - Test mobile menu toggle: hamburger icon visible initially, click toggles mobile nav

    utils.test.ts:
    - formatPrice: $10.00 for 10, $0.50 for 0.5, "Free" for null, "Free" for undefined
    - formatDate: returns formatted string for valid date, "" for undefined/empty
    - cn: joins truthy classes, filters falsy (false, null, undefined)

    auth.test.ts:
    - Use vi.stubGlobal to mock localStorage (or use a simple object store pattern)
    - saveAuth: stores token with key "shareshelf_token", stores user JSON with key "shareshelf_user"
    - getToken: returns stored token, returns null when not set
    - getUser: parses and returns user object, returns null when not set
    - clearAuth: removes both keys
    - isAuthenticated: returns true when token exists, false when not

    api.test.ts:
    - Mock axios.create to return a mock instance
    - Test baseURL falls back to "/api" when NEXT_PUBLIC_API_URL is undefined
    - Test request interceptor injects Authorization header from localStorage
    - Test response interceptor: 401 clears auth and redirects to /login when not already on /login
    - Test response interceptor: non-401 errors pass through (rejected promise)
  </action>

  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/__tests__/ src/lib/__tests__/ 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - 4 test files exist: Navbar.test.tsx, utils.test.ts, auth.test.ts, api.test.ts
    - `npx vitest run` passes all 4 test files
    - Navbar tests cover logged-in, logged-out, and mobile toggle states
    - utils tests cover formatPrice (value/null/undefined), formatDate, cn joining
    - auth tests cover saveAuth, getToken, getUser, clearAuth, isAuthenticated via localStorage
    - api tests cover baseURL fallback, JWT injection, 401 redirect, non-401 passthrough
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> source components | Unit tests import and render UI components. No network or stored data crosses here. |
| test code -> localStorage | auth.test.ts reads/writes localStorage via mock. No real storage accessed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm installs (vitest, testing-library, jsdom) | mitigate | Verify install success via `npx vitest run` exit code 0. All packages are top-1000 npm downloads with no known supply-chain incidents. |
</threat_model>

<verification>
- `npx vitest run` exits 0 with all component and lib tests passing
- 10 test files created across components/ui, components/layout, lib directories
- No source files modified (only test files and config)
</verification>

<success_criteria>
- Frontend test infrastructure is operational (`npx vitest run` works)
- All 10+ test files pass covering ALL UI component states and lib functions per UI-SPEC.md
- Tests use Vitest + RTL patterns that subsequent plans can replicate for page tests
</success_criteria>

<output>
Create `.planning/phases/01-fix-critical-issues-testing-foundation/01-01-SUMMARY.md` when done
</output>
