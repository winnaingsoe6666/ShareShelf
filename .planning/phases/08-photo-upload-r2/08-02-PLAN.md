---
phase: 08-photo-upload-r2
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/next.config.ts
  - frontend/src/components/items/ItemCard.tsx
  - frontend/src/app/[locale]/items/[id]/page.tsx
  - frontend/src/app/[locale]/items/new/page.tsx
  - frontend/src/lib/api.ts
  - backend/src/main/resources/application-dev.yml
autonomous: true
requirements:
  - R2-02
must_haves:
  truths:
    - "next.config.ts image.remotePatterns includes R2 public domain"
    - "ItemCard and item detail page render images from R2 URLs"
    - "Frontend upload flow works unchanged (same API contract)"
    - "application-dev.yml has local R2 defaults (or S3-compatible local mock)"
    - "Backend compiles and starts with R2 config"
  artifacts:
    - path: "frontend/next.config.ts"
      provides: "R2 image domain in remotePatterns"
      contains: "r2"
    - path: "frontend/src/components/items/ItemCard.tsx"
      provides: "R2 image rendering"
      contains: "imageUrls"
  key_links:
    - from: "next.config.ts"
      to: "ItemCard.tsx"
      via: "Image component remotePatterns allow R2 domain"
      pattern: "remotePatterns.*r2"
    - from: "FileStorageService.kt"
      to: "ItemCard.tsx"
      via: "Store returns full R2 URL, frontend renders it"
      pattern: "publicUrl.*imageUrls"
---

<objective>
Update the frontend to render images from R2 public URLs and ensure the end-to-end upload flow works with the new R2 backend.

Purpose: After 08-01 swaps the storage backend to R2, the frontend Next.js Image component needs to allow the R2 domain in remotePatterns. The item card and detail page must render R2-hosted images correctly. The upload API contract is unchanged so frontend upload code needs no logic changes — only the image URL format changes (from relative /uploads/... to full https://r2-domain/...).

Output: Updated next.config.ts, verified image rendering from R2, dev environment R2 config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-photo-upload-r2/08-01-PLAN.md
@frontend/next.config.ts
@frontend/src/components/items/ItemCard.tsx
@frontend/src/app/[locale]/items/[id]/page.tsx
@frontend/src/app/[locale]/items/new/page.tsx
@frontend/src/lib/api.ts
@backend/src/main/resources/application-dev.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update next.config.ts for R2 image domain</name>
  <files>
    frontend/next.config.ts
  </files>
  <behavior>
    - images.remotePatterns includes the R2 public domain
    - Existing localhost and railway.app patterns remain
    - Next.js Image component can render images from R2 URLs
  </behavior>
  <action>
    1. Read `frontend/next.config.ts`
    2. Add a new entry to `images.remotePatterns` for the R2 public domain:
       ```typescript
       {
         protocol: "https",
         hostname: "**.r2.cloudflarestorage.com",
       },
       ```
       Or if using a custom domain for R2:
       ```typescript
       {
         protocol: "https",
         hostname: "images.shareshelf.com",  // or whatever the R2 custom domain is
       },
       ```
    3. Also add the R2 public URL hostname dynamically if it varies — use a wildcard pattern or the specific domain from R2_PUBLIC_URL
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/frontend && npx next lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts images.remotePatterns includes R2 domain
    - Existing patterns for localhost:8080 and *.railway.app preserved
    - `npx next lint` passes (or no new errors)
  </acceptance_criteria>
  <done>next.config.ts allows Next.js Image to render from R2 public domain</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify frontend image rendering with full R2 URLs</name>
  <files>
    frontend/src/components/items/ItemCard.tsx,
    frontend/src/app/[locale]/items/[id]/page.tsx,
    frontend/src/app/[locale]/items/new/page.tsx
  </files>
  <behavior>
    - ItemCard renders image from item.imageUrls[0] which is now a full R2 URL
    - Item detail page renders image gallery from full R2 URLs
    - ImageUpload component displays existing images from R2 URLs
    - No logic changes needed — URLs are just strings, the Image component handles them
  </behavior>
  <action>
    1. Read ItemCard.tsx — verify it passes `imageUrls[0]` to Next.js `Image` component's `src` prop
       - If it uses a relative path prefix (e.g., `${process.env.NEXT_PUBLIC_API_URL}${url}`), REMOVE that prefix — R2 URLs are already absolute
       - If it already uses the URL directly, no change needed
    2. Read item detail page — same check: ensure no `/api` prefix is prepended to image URLs
    3. Read item creation page — verify upload flow still works (POST /items/{id}/images with FormData)
    4. If any component prepends the API base URL to image paths, fix it to use the URL as-is
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/frontend && grep -rn "NEXT_PUBLIC_API_URL.*imageUrl\|api.*imageUrl\|/uploads/" src/ 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - No component prepends API base URL to image URLs from the backend
    - No hardcoded `/uploads/` paths remain in frontend code
    - ItemCard, item detail, and item creation pages handle full R2 URLs correctly
  </acceptance_criteria>
  <done>Frontend renders images from full R2 URLs without path manipulation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dev environment R2 configuration</name>
  <files>
    backend/src/main/resources/application-dev.yml
  </files>
  <behavior>
    - application-dev.yml has R2 defaults for local development
    - Developers can use Cloudflare R2 free tier or a local S3-compatible mock (MinIO)
    - Environment variables override defaults when set
  </behavior>
  <action>
    1. Read `backend/src/main/resources/application-dev.yml` (create if missing)
    2. Add R2 dev configuration:
       ```yaml
       app:
         r2:
           endpoint: ${R2_ENDPOINT:https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com}
           access-key-id: ${R2_ACCESS_KEY_ID:your-dev-key}
           secret-access-key: ${R2_SECRET_ACCESS_KEY:your-dev-secret}
           bucket: ${R2_BUCKET:shareshelf-dev}
           public-url: ${R2_PUBLIC_URL:https://your-dev-bucket.YOUR_ACCOUNT_ID.r2.cloudflarestorage.com}
       ```
    3. Document in a comment that developers need to set up a Cloudflare R2 bucket and provide credentials via env vars
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/backend && ./gradlew compileKotlin 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - application-dev.yml has R2 configuration with env var fallbacks
    - Backend compiles and can start with R2 config (will fail on actual R2 connection if no credentials, but config is valid)
  </acceptance_criteria>
  <done>Dev environment R2 configuration in place with env var override support</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx next lint` — no lint errors
2. `grep -rn "/uploads/" frontend/src/` — no hardcoded local upload paths
3. `grep "r2" frontend/next.config.ts` — R2 domain in image remotePatterns
4. `grep "r2" backend/src/main/resources/application-dev.yml` — dev R2 config present
5. Backend starts without config errors (with R2 env vars set or defaults)
</verification>

<success_criteria>
- Next.js Image component allows R2 public domain
- Frontend image rendering works with full R2 URLs (no path prepending)
- No hardcoded /uploads/ paths in frontend
- Dev environment has R2 configuration
- API contract unchanged — frontend upload code needs no logic changes
</success_criteria>

<output>
Create `.planning/phases/08-photo-upload-r2/08-02-SUMMARY.md` when done
</output>
