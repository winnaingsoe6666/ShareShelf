---
phase: 07-google-oauth-signup
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - backend/src/main/kotlin/com/shareshelf/config/SecurityConfig.kt
  - backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationSuccessHandler.kt
  - backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationFailureHandler.kt
  - backend/src/test/kotlin/com/shareshelf/auth/OAuth2ServiceTest.kt
autonomous: true
requirements:
  - OAUTH-01
  - OAUTH-03
must_haves:
  truths:
    - "SecurityConfig permits /oauth2/** and /login/oauth2/** endpoints"
    - "SecurityConfig adds oauth2Login() to the filter chain"
    - "OAuth2 success handler generates JWT and redirects to frontend /auth/callback"
    - "OAuth2 failure handler redirects to frontend /login with error param"
    - "Cookie-based AuthorizationRequestRepository is used (no HTTP sessions)"
    - "Backend tests verify user creation, linking, and JWT generation"
  artifacts:
    - path: "backend/src/main/kotlin/com/shareshelf/config/SecurityConfig.kt"
      provides: "OAuth2 login filter chain configuration"
      contains: "oauth2Login"
    - path: "backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationSuccessHandler.kt"
      provides: "JWT generation on OAuth success"
      contains: "oauth2/callback"
    - path: "backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationFailureHandler.kt"
      provides: "Error redirect on OAuth failure"
      contains: "google_auth_failed"
    - path: "backend/src/test/kotlin/com/shareshelf/auth/OAuth2ServiceTest.kt"
      provides: "Unit tests for OAuth2Service"
      contains: "processOAuthUser"
  key_links:
    - from: "SecurityConfig.kt"
      to: "OAuth2AuthenticationSuccessHandler.kt"
      via: "successHandler bean injection"
      pattern: "successHandler"
    - from: "SecurityConfig.kt"
      to: "OAuth2AuthenticationFailureHandler.kt"
      via: "failureHandler bean injection"
      pattern: "failureHandler"
    - from: "OAuth2AuthenticationSuccessHandler.kt"
      to: "OAuth2Service.kt"
      via: "processOAuthUser call"
      pattern: "processOAuthUser"
---

<objective>
Wire OAuth2 login into Spring Security filter chain and create success/failure handlers.

Purpose: SecurityConfig is where the OAuth2 login filter is registered. The success handler is the critical bridge — it receives the authenticated OAuth2User from Google, calls OAuth2Service to find/create the user, generates a JWT, and redirects to the frontend callback page. Backend tests verify the service logic.
Output: Updated SecurityConfig, new OAuth2AuthenticationSuccessHandler, new OAuth2AuthenticationFailureHandler, new OAuth2ServiceTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-google-oauth-signup/07-CONTEXT.md
@.planning/phases/07-google-oauth-signup/07-RESEARCH.md
@.planning/phases/07-google-oauth-signup/07-01-SUMMARY.md
@.planning/phases/07-google-oauth-signup/07-02-SUMMARY.md
@backend/src/main/kotlin/com/shareshelf/config/SecurityConfig.kt
@backend/src/main/kotlin/com/shareshelf/auth/OAuth2Service.kt
@backend/src/main/kotlin/com/shareshelf/auth/AuthService.kt
@backend/src/main/kotlin/com/shareshelf/auth/JwtTokenProvider.kt
@backend/src/main/kotlin/com/shareshelf/auth/entity/User.kt
@backend/src/main/kotlin/com/shareshelf/auth/entity/UserRepository.kt

<interfaces>
From backend/src/main/kotlin/com/shareshelf/auth/OAuth2Service.kt (Plan 02 creates):
```kotlin
@Service
class OAuth2Service(
    private val userRepository: UserRepository,
    private val jwtTokenProvider: JwtTokenProvider,
    private val refreshTokenRepository: RefreshTokenRepository,
    @Value("\${jwt.refresh-expiration-ms}") private val refreshExpirationMs: Long
) {
    fun processOAuthUser(oAuth2User: OAuth2User): User
    fun generateOAuthResponse(user: User): AuthResponse
}
```

From backend/src/main/kotlin/com/shareshelf/auth/dto/AuthDtos.kt:
```kotlin
data class AuthResponse(
    val token: String,
    val refreshToken: String,
    val userId: Long,
    val name: String,
    val email: String,
    val trustScore: Double,
    val community: String?,
    val avatarUrl: String?
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OAuth2 success and failure handlers</name>
  <files>
    backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationSuccessHandler.kt,
    backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationFailureHandler.kt
  </files>
  <behavior>
    - Success handler extracts OAuth2User from Authentication, calls oAuth2Service.processOAuthUser, calls oAuth2Service.generateOAuthResponse, redirects to {frontendUrl}/auth/callback?token={jwt}&refreshToken={refresh}&returnUrl={returnUrl}
    - Success handler reads returnUrl from cookie "oauth_return_url" set during authorization request
    - Failure handler redirects to {frontendUrl}/login?error=google_auth_failed
    - Both handlers use @Value("${app.frontend-url}") for the frontend base URL
  </behavior>
  <action>
    1. Create `OAuth2AuthenticationSuccessHandler.kt` in `backend/src/main/kotlin/com/shareshelf/auth/` (per D-03, D-04):
       - `@Component class OAuth2AuthenticationSuccessHandler(private val oAuth2Service: OAuth2Service, @Value("\${app.frontend-url}") private val frontendUrl: String) : AuthenticationSuccessHandler`
       - In `onAuthenticationSuccess(request, response, authentication)`:
         - Cast `authentication.principal` to `OAuth2User`
         - Call `oAuth2Service.processOAuthUser(oAuth2User)` to get/create User
         - Call `oAuth2Service.generateOAuthResponse(user)` to get AuthResponse
         - Read `returnUrl` from cookie named `oauth_return_url` (if present, use it; otherwise empty)
         - Build redirect URL: `${frontendUrl}/auth/callback?token=${authResponse.token}&refreshToken=${authResponse.refreshToken}&returnUrl=${returnUrl}`
         - Call `response.sendRedirect(redirectUrl)`
       - Import: `org.springframework.security.oauth2.core.user.OAuth2User`, `org.springframework.security.web.authentication.AuthenticationSuccessHandler`, `jakarta.servlet.http.Cookie`

    2. Create `OAuth2AuthenticationFailureHandler.kt` in `backend/src/main/kotlin/com/shareshelf/auth/` (per D-03):
       - `@Component class OAuth2AuthenticationFailureHandler(@Value("\${app.frontend-url}") private val frontendUrl: String) : AuthenticationFailureHandler`
       - In `onAuthenticationFailure(request, response, exception)`:
         - Redirect to `${frontendUrl}/login?error=google_auth_failed`
         - Call `response.sendRedirect(redirectUrl)`
       - Import: `org.springframework.security.web.authentication.AuthenticationFailureHandler`
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/backend && ./gradlew compileKotlin 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - OAuth2AuthenticationSuccessHandler.kt exists with @Component and implements AuthenticationSuccessHandler
    - onAuthenticationSuccess calls processOAuthUser and generateOAuthResponse
    - Redirect URL includes token, refreshToken, and returnUrl query params
    - OAuth2AuthenticationFailureHandler.kt exists with @Component and implements AuthenticationFailureHandler
    - onAuthenticationFailure redirects to /login?error=google_auth_failed
    - Both handlers read frontendUrl from app.frontend-url property
    - `cd backend && ./gradlew compileKotlin` succeeds
  </acceptance_criteria>
  <done>Success handler generates JWT and redirects to frontend callback; failure handler redirects to login with error</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update SecurityConfig with OAuth2 login and write OAuth2Service tests</name>
  <files>
    backend/src/main/kotlin/com/shareshelf/config/SecurityConfig.kt,
    backend/src/test/kotlin/com/shareshelf/auth/OAuth2ServiceTest.kt
  </files>
  <behavior>
    - SecurityConfig permits /oauth2/**, /login/oauth2/** endpoints
    - SecurityConfig adds oauth2Login() with custom success/failure handlers
    - OAuth2Service tests verify: find by Google ID returns existing user; auto-link by email sets googleId and authProvider; create new user sets correct defaults; generateOAuthResponse returns valid AuthResponse
  </behavior>
  <action>
    1. Modify `SecurityConfig.kt` (per D-01, D-02, D-03):
       - Add constructor params: `private val oAuth2SuccessHandler: OAuth2AuthenticationSuccessHandler`, `private val oAuth2FailureHandler: OAuth2AuthenticationFailureHandler`
       - In `authorizeHttpRequests`, add `.requestMatchers("/oauth2/**", "/login/oauth2/**").permitAll()` to the permit list
       - After `.addFilterBefore(rateLimitFilter, ...)`, add:
         ```
         .oauth2Login { oauth2 ->
             oauth2
                 .authorizationEndpoint { it.baseUri("/oauth2/authorization") }
                 .redirectionEndpoint { it.baseUri("/oauth2/callback/*") }
                 .successHandler(oAuth2SuccessHandler)
                 .failureHandler(oAuth2FailureHandler)
         }
         ```
       - Import: `com.shareshelf.auth.OAuth2AuthenticationSuccessHandler`, `com.shareshelf.auth.OAuth2AuthenticationFailureHandler`

    2. Create `OAuth2ServiceTest.kt` in `backend/src/test/kotlin/com/shareshelf/auth/`:
       - Use MockK to mock `UserRepository`, `JwtTokenProvider`, `RefreshTokenRepository`
       - Test cases:
         a. `processOAuthUser - finds by Google ID`: Mock `findByGoogleId("google123")` to return existing user. Verify it returns that user without calling save.
         b. `processOAuthUser - auto-links by email`: Mock `findByGoogleId` returns null, `findByEmail("alice@gmail.com")` returns LOCAL user. Verify user's `googleId` and `authProvider` are updated and `save()` is called.
         c. `processOAuthUser - creates new user`: Mock both lookups return null. Verify `save()` is called with User having `authProvider = GOOGLE`, `passwordHash = ""`, correct `googleId`, `email`, `name`.
         d. `processOAuthUser - imports avatar`: Mock existing LOCAL user with `avatarUrl = null`. Verify `avatarUrl` is set from Google's `picture` attribute.
       - Use `every { mockOAuth2User.getAttribute<String>("sub") } returns "google123"` pattern for mocking OAuth2User
       - Use `@Value` annotation reflection or constructor parameter for refreshExpirationMs test value
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/backend && ./gradlew test --tests "com.shareshelf.auth.OAuth2ServiceTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SecurityConfig has `oauth2Login()` in the filter chain
    - `/oauth2/**` and `/login/oauth2/**` are in the permitAll list
    - OAuth2AuthenticationSuccessHandler and OAuth2AuthenticationFailureHandler are injected into SecurityConfig
    - OAuth2ServiceTest.kt has 4+ test cases covering all processOAuthUser branches
    - All OAuth2ServiceTest tests pass
    - `cd backend && ./gradlew test --tests "com.shareshelf.auth.OAuth2ServiceTest"` exits with 0
  </acceptance_criteria>
  <done>SecurityConfig wired with OAuth2 login filter; all /oauth2/** endpoints permitted; OAuth2ServiceTest covers find/link/create/avatar-import logic</done>
</task>

</tasks>

<verification>
1. `cd backend && ./gradlew compileKotlin` — compiles without errors
2. `cd backend && ./gradlew test --tests "com.shareshelf.auth.OAuth2ServiceTest"` — all tests pass
3. `grep -c "oauth2Login" backend/src/main/kotlin/com/shareshelf/config/SecurityConfig.kt` — returns 1
4. `grep -c "permitAll" backend/src/main/kotlin/com/shareshelf/config/SecurityConfig.kt` — includes /oauth2/** entries
5. `grep -c "redirect" backend/src/main/kotlin/com/shareshelf/auth/OAuth2AuthenticationSuccessHandler.kt` — redirect logic present
</verification>

<success_criteria>
- SecurityConfig permits /oauth2/** and /login/oauth2/** endpoints without authentication
- oauth2Login() is configured with cookie-based authorization endpoint, custom success and failure handlers
- OAuth2AuthenticationSuccessHandler calls processOAuthUser, generates JWT, and redirects to frontend /auth/callback with token params
- OAuth2AuthenticationFailureHandler redirects to frontend /login?error=google_auth_failed
- OAuth2ServiceTest has 4+ passing test cases covering all user creation/linking branches
- Existing security config (JWT filter, rate limit filter) is preserved
</success_criteria>

<output>
Create `.planning/phases/07-google-oauth-signup/07-03-SUMMARY.md` when done
</output>
