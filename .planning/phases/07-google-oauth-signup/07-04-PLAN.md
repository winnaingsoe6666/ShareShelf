---
phase: 07-google-oauth-signup
plan: 04
type: execute
wave: 4
depends_on:
  - 07-02
  - 07-03
files_modified:
  - frontend/src/components/ui/GoogleSignInButton.tsx
  - frontend/src/components/ui/AuthDivider.tsx
  - frontend/src/app/[locale]/auth/callback/page.tsx
  - frontend/src/app/[locale]/login/page.tsx
  - frontend/src/app/[locale]/register/page.tsx
  - frontend/src/components/ui/__tests__/GoogleSignInButton.test.tsx
  - frontend/src/components/ui/__tests__/AuthDivider.test.tsx
  - frontend/src/app/[locale]/auth/callback/__tests__/AuthCallbackPage.test.tsx
autonomous: true
requirements:
  - OAUTH-03
  - OAUTH-04
must_haves:
  truths:
    - "GoogleSignInButton links to /api/oauth2/authorization/google"
    - "AuthDivider shows 'or' between Google button and form"
    - "Login page has Google button above email/password form"
    - "Register page has Google button above email/password form"
    - "AuthCallback page reads token/refreshToken from URL params and calls saveAuth"
    - "AuthCallback shows error state when params missing"
    - "Google button uses official Google branding (G logo, colors)"
  artifacts:
    - path: "frontend/src/components/ui/GoogleSignInButton.tsx"
      provides: "Google-branded OAuth button"
      contains: "oauth2/authorization/google"
    - path: "frontend/src/components/ui/AuthDivider.tsx"
      provides: "Or divider between auth methods"
      contains: "or"
    - path: "frontend/src/app/[locale]/auth/callback/page.tsx"
      provides: "OAuth callback handler page"
      contains: "saveAuth"
    - path: "frontend/src/app/[locale]/login/page.tsx"
      provides: "Login page with Google button"
      contains: "GoogleSignInButton"
    - path: "frontend/src/app/[locale]/register/page.tsx"
      provides: "Register page with Google button"
      contains: "GoogleSignInButton"
  key_links:
    - from: "GoogleSignInButton.tsx"
      to: "SecurityConfig.kt"
      via: "Button href matches oauth2Login baseUri"
      pattern: "/api/oauth2/authorization/google"
    - from: "AuthCallback page"
      to: "OAuth2AuthenticationSuccessHandler.kt"
      via: "URL params token/refreshToken match redirect"
      pattern: "token.*refreshToken"
    - from: "login/page.tsx"
      to: "GoogleSignInButton.tsx"
      via: "Import and render above form"
      pattern: "GoogleSignInButton"
---

<objective>
Add "Sign in with Google" button to login and register pages, create AuthDivider component, and build the /auth/callback page that handles the OAuth2 redirect.

Purpose: Complete the frontend side of Google OAuth — users see the Google button, click it, authenticate with Google, and the callback page saves the JWT and redirects.
Output: GoogleSignInButton component, AuthDivider component, AuthCallbackPage, modified login and register pages, frontend tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-google-oauth-signup/07-CONTEXT.md
@.planning/phases/07-google-oauth-signup/07-RESEARCH.md
@.planning/phases/07-google-oauth-signup/07-UI-SPEC.md
@frontend/src/app/[locale]/login/page.tsx
@frontend/src/app/[locale]/register/page.tsx
@frontend/src/lib/auth.ts
@frontend/src/types/index.ts
@frontend/src/components/ui/Button.tsx
@frontend/src/components/ui/Input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GoogleSignInButton and AuthDivider components</name>
  <files>
    frontend/src/components/ui/GoogleSignInButton.tsx,
    frontend/src/components/ui/AuthDivider.tsx
  </files>
  <read_first>
    frontend/src/components/ui/Button.tsx,
    frontend/src/app/[locale]/login/page.tsx
  </read_first>
  <behavior>
    - GoogleSignInButton is a client component that renders an anchor tag linking to /api/oauth2/authorization/google
    - On click, before navigating, sets cookie "oauth_return_url" with current window.location.pathname + search (per D-14 return URL preservation)
    - Button shows official Google "G" SVG logo (20x20px, multi-color) and "Sign in with Google" text
    - Styling per UI-SPEC: w-full flex items-center justify-center gap-3 px-4 py-2.5 rounded-lg border border-gray-300 bg-white text-gray-700 text-sm font-medium shadow-sm hover:bg-gray-50 hover:shadow-md transition-all duration-200 cursor-pointer
    - Google "G" logo is an inline SVG with official Google colors (#4285F4, #34A853, #FBBC05, #EA4335)
    - aria-label="Sign in with Google" on the link
    - Focus ring: focus:ring-2 focus:ring-purple-500 focus:ring-offset-2
    - AuthDivider renders flex container with two flex-1 h-px bg-purple-200 lines and "or" text in text-sm text-stone-500
    - Both components are default-exported function components with TypeScript interfaces
  </behavior>
  <action>
    1. Create `frontend/src/components/ui/GoogleSignInButton.tsx`:
       - Interface: `interface GoogleSignInButtonProps { text?: string; className?: string }`
       - Default export function component
       - Add "use client" directive at top
       - Add onClick handler that sets `document.cookie = "oauth_return_url=" + encodeURIComponent(window.location.pathname + window.location.search) + ";path=/;max-age=600;SameSite=Lax"` before allowing navigation (per D-14)
       - Renders: `<a href="/api/oauth2/authorization/google" onClick={handleClick} aria-label={text} className="w-full flex items-center justify-center gap-3 px-4 py-2.5 rounded-lg border border-gray-300 bg-white text-gray-700 text-sm font-medium shadow-sm hover:bg-gray-50 hover:shadow-md transition-all duration-200 cursor-pointer focus:ring-2 focus:ring-purple-500 focus:ring-offset-2 ...className">`
       - Inside: Google "G" SVG (20x20 viewBox, official multi-color paths) + `<span>{text}</span>`
       - Default text: "Sign in with Google"

    2. Create `frontend/src/components/ui/AuthDivider.tsx`:
       - Default export function component, no props needed
       - Renders: `<div className="flex items-center gap-4"><div className="flex-1 h-px bg-purple-200" /><span className="text-sm text-stone-500">or</span><div className="flex-1 h-px bg-purple-200" /></div>`
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/GoogleSignInButton.tsx exists with "use client" directive
    - GoogleSignInButton renders an anchor with href="/api/oauth2/authorization/google"
    - GoogleSignInButton onClick sets oauth_return_url cookie with current page path
    - GoogleSignInButton includes inline SVG with Google "G" logo
    - GoogleSignInButton has aria-label attribute
    - frontend/src/components/ui/AuthDivider.tsx exists
    - AuthDivider renders "or" text between two horizontal lines
    - Both components compile without TypeScript errors
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GoogleSignInButton and AuthDivider components created with correct styling and accessibility</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthCallback page</name>
  <files>
    frontend/src/app/[locale]/auth/callback/page.tsx
  </files>
  <read_first>
    frontend/src/lib/auth.ts,
    frontend/src/app/[locale]/login/page.tsx,
    frontend/src/types/index.ts
  </read_first>
  <behavior>
    - Page reads token, refreshToken, and returnUrl from URL search params on mount
    - If token and refreshToken present: construct AuthResponse object, call saveAuth(), then router.push(returnUrl || "/items")
    - If params missing: show error state with AlertCircle icon, "Authentication failed" heading, "Something went wrong during sign-in. Please try again." body, and "Back to Sign In" button linking to /login
    - Loading state: centered card with Loader2 spinning icon, "Signing you in..." heading, "Please wait while we complete your authentication." body
    - Background: min-h-screen bg-purple-50
    - Card: bg-white rounded-2xl shadow-xl p-8 text-center max-w-md
    - Uses "use client" directive
    - Uses useRouter from @/i18n/navigation and useSearchParams
  </behavior>
  <action>
    1. Create directory: `frontend/src/app/[locale]/auth/callback/`
    2. Create `frontend/src/app/[locale]/auth/callback/page.tsx`:
       - "use client" directive
       - Import: `useEffect, useState` from react, `useRouter` from @/i18n/navigation, `useSearchParams` from next/navigation, `Loader2, AlertCircle` from lucide-react, `saveAuth` from @/lib/auth, `Button` from @/components/ui/Button
       - Interface for page state: loading | error
       - On mount (useEffect): read `token`, `refreshToken`, `returnUrl` from searchParams
         - If both present: call `saveAuth({ token, refreshToken, userId: 0, name: "", email: "", trustScore: 0 })`, then `router.push(returnUrl || "/items")`
         - If missing: set error state
       - Loading state: `<div className="min-h-screen bg-purple-50 flex items-center justify-center"><div className="bg-white rounded-2xl shadow-xl p-8 text-center max-w-md"><Loader2 className="mx-auto h-10 w-10 text-purple-400 animate-spin" /><h1 className="mt-4 font-heading text-2xl font-bold text-purple-900">Signing you in...</h1><p className="mt-2 text-sm text-stone-600">Please wait while we complete your authentication.</p></div></div>`
       - Error state: same layout but with `<AlertCircle className="mx-auto h-10 w-10 text-red-400" />`, heading "Authentication failed", body "Something went wrong during sign-in. Please try again.", and `<Link href="/login"><Button variant="outline">Back to Sign In</Button></Link>`
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/[locale]/auth/callback/page.tsx exists with "use client" directive
    - Page reads token and refreshToken from URL search params
    - Page calls saveAuth() when both tokens present
    - Page redirects to returnUrl or "/items" after saving auth
    - Error state shows when token params are missing
    - Error state includes "Back to Sign In" link to /login
    - Loading state shows Loader2 spinning icon
    - Page uses min-h-screen bg-purple-50 background
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AuthCallback page handles OAuth redirect with loading and error states</done>
</task>

<task type="auto">
  <name>Task 3: Add Google button to login and register pages</name>
  <files>
    frontend/src/app/[locale]/login/page.tsx,
    frontend/src/app/[locale]/register/page.tsx
  </files>
  <read_first>
    frontend/src/app/[locale]/login/page.tsx,
    frontend/src/app/[locale]/register/page.tsx,
    frontend/src/components/ui/GoogleSignInButton.tsx,
    frontend/src/components/ui/AuthDivider.tsx
  </read_first>
  <behavior>
    - Login page imports GoogleSignInButton and AuthDivider
    - Google button appears between the header div and the form element
    - AuthDivider appears between Google button and form
    - Header mb-8 changed to mb-6 to accommodate new elements
    - Register page has identical changes
    - Login page checks for error query param and shows Google auth failure alert
    - Register page text says "Sign up with Google" instead of "Sign in with Google"
  </behavior>
  <action>
    1. Modify `frontend/src/app/[locale]/login/page.tsx`:
       - Add imports: `import GoogleSignInButton from "@/components/ui/GoogleSignInButton"` and `import AuthDivider from "@/components/ui/AuthDivider"` and `import { useSearchParams } from "next/navigation"`
       - Add `const searchParams = useSearchParams()` inside the component
       - Change header div `className="mb-8 text-center"` to `className="mb-6 text-center"`
       - After the header closing `</div>` and before the `<form>` tag, insert:
         `<GoogleSignInButton text="Sign in with Google" />`
         `<AuthDivider />`
       - Add error alert for Google auth failure: after the existing `{error && ...}` block inside the form, add:
         `{searchParams.get("error") === "google_auth_failed" && !error && (<div className="rounded-lg bg-red-50 px-4 py-3 text-sm text-red-700">Google sign-in failed. Please try again.</div>)}`

    2. Modify `frontend/src/app/[locale]/register/page.tsx`:
       - Same changes as login page but with text="Sign up with Google"
       - Add same imports for GoogleSignInButton, AuthDivider, useSearchParams
       - Same error param handling for google_auth_failed
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - login/page.tsx imports GoogleSignInButton and AuthDivider
    - login/page.tsx renders GoogleSignInButton above the form with text="Sign in with Google"
    - login/page.tsx renders AuthDivider between Google button and form
    - login/page.tsx checks for error=google_auth_failed query param
    - register/page.tsx imports and renders Google button with text="Sign up with Google"
    - register/page.tsx has same divider and error handling
    - Header spacing changed from mb-8 to mb-6 on both pages
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Login and register pages show Google Sign-In button with divider above the form</done>
</task>

<task type="auto">
  <name>Task 4: Add frontend tests for OAuth components</name>
  <files>
    frontend/src/components/ui/__tests__/GoogleSignInButton.test.tsx,
    frontend/src/components/ui/__tests__/AuthDivider.test.tsx,
    frontend/src/app/[locale]/auth/callback/__tests__/AuthCallbackPage.test.tsx
  </files>
  <read_first>
    frontend/src/components/ui/GoogleSignInButton.tsx,
    frontend/src/components/ui/AuthDivider.tsx,
    frontend/src/app/[locale]/auth/callback/page.tsx
  </read_first>
  <behavior>
    - GoogleSignInButton test: renders link to /api/oauth2/authorization/google, shows "Sign in with Google" text, has aria-label
    - AuthDivider test: renders "or" text
    - AuthCallbackPage test: shows loading state initially, reads tokens from URL params
  </behavior>
  <action>
    1. Create `frontend/src/components/ui/__tests__/GoogleSignInButton.test.tsx`:
       - Import render, screen from @testing-library/react
       - Test "renders Google sign-in link": render component, check link href="/api/oauth2/authorization/google"
       - Test "shows default text": check screen.getByText("Sign in with Google")
       - Test "has aria-label": check link has aria-label="Sign in with Google"
       - Test "shows custom text": render with text="Sign up with Google", check text

    2. Create `frontend/src/components/ui/__tests__/AuthDivider.test.tsx`:
       - Test "renders or text": render component, check screen.getByText("or")

    3. Create `frontend/src/app/[locale]/auth/callback/__tests__/AuthCallbackPage.test.tsx`:
       - Mock next/navigation useSearchParams
       - Mock @/lib/auth saveAuth
       - Mock @/i18n/navigation useRouter
       - Test "shows loading state": render component, check for "Signing you in..."
       - Test "shows error when no params": mock empty search params, check for "Authentication failed"
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/frontend && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - GoogleSignInButton test file exists with 3+ test cases
    - AuthDivider test file exists with 1+ test case
    - AuthCallbackPage test file exists with 2+ test cases
    - All tests pass: `cd frontend && npx vitest run` exits 0
  </acceptance_criteria>
  <done>Frontend tests cover Google button, divider, and callback page</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd frontend && npx vitest run` — all tests pass
3. `grep -r "GoogleSignInButton" frontend/src/app/[locale]/login/page.tsx` — button imported and used
4. `grep -r "GoogleSignInButton" frontend/src/app/[locale]/register/page.tsx` — button imported and used
5. `grep -r "saveAuth" frontend/src/app/[locale]/auth/callback/page.tsx` — callback saves auth
6. `grep -r "oauth2/authorization/google" frontend/src/components/ui/GoogleSignInButton.tsx` — correct OAuth URL
</verification>

<success_criteria>
- GoogleSignInButton renders with Google "G" logo and links to /api/oauth2/authorization/google
- AuthDivider shows "or" between horizontal lines
- Login page displays Google button above email/password form with "Sign in with Google"
- Register page displays Google button above email/password form with "Sign up with Google"
- AuthCallback page reads token/refreshToken from URL params and calls saveAuth()
- AuthCallback shows error state when params missing
- AuthCallback shows loading state with spinner
- Google auth failure error shown when ?error=google_auth_failed in URL
- All frontend TypeScript compiles
- All frontend tests pass
</success_criteria>

<output>
Create `.planning/phases/07-google-oauth-signup/07-04-SUMMARY.md` when done
</output>
