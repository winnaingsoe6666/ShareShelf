---
phase: 07-google-oauth-signup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V13__add_google_oauth.sql
  - backend/src/main/kotlin/com/shareshelf/auth/entity/User.kt
  - backend/src/main/kotlin/com/shareshelf/auth/entity/UserRepository.kt
  - backend/src/main/kotlin/com/shareshelf/auth/entity/AuthProvider.kt
  - backend/src/main/kotlin/com/shareshelf/auth/AuthService.kt
autonomous: true
requirements:
  - OAUTH-02
must_haves:
  truths:
    - "Users table has google_id and auth_provider columns"
    - "passwordHash is nullable for Google-only users"
    - "UserRepository can look up users by Google ID"
    - "AuthService.login() rejects password login for Google-only users"
    - "Existing LOCAL users are unaffected by migration"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V13__add_google_oauth.sql"
      provides: "Google OAuth schema columns"
      contains: "google_id"
    - path: "backend/src/main/kotlin/com/shareshelf/auth/entity/User.kt"
      provides: "googleId and authProvider fields"
      contains: "authProvider"
    - path: "backend/src/main/kotlin/com/shareshelf/auth/entity/UserRepository.kt"
      provides: "findByGoogleId method"
      contains: "findByGoogleId"
  key_links:
    - from: "V13__add_google_oauth.sql"
      to: "User.kt"
      via: "Column annotations match migration"
      pattern: "google_id.*auth_provider"
    - from: "AuthService.kt"
      to: "User.authProvider"
      via: "Login checks auth provider before password"
      pattern: "authProvider.*GOOGLE"
---

<objective>
Add Google OAuth schema support to the database and User entity.

Purpose: Lay the data foundation for OAuth login — users can be identified by Google ID, and Google-only users can exist without a password hash.
Output: Flyway migration V13, updated User entity with googleId/authProvider fields, UserRepository with findByGoogleId, AuthService login guard for Google-only users.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-google-oauth-signup/07-CONTEXT.md
@.planning/phases/07-google-oauth-signup/07-RESEARCH.md
@backend/src/main/kotlin/com/shareshelf/auth/entity/User.kt
@backend/src/main/kotlin/com/shareshelf/auth/entity/UserRepository.kt
@backend/src/main/kotlin/com/shareshelf/auth/AuthService.kt
@backend/src/main/resources/application.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flyway migration + User entity + UserRepository + AuthService login guard</name>
  <files>
    backend/src/main/resources/db/migration/V13__add_google_oauth.sql,
    backend/src/main/kotlin/com/shareshelf/auth/entity/AuthProvider.kt,
    backend/src/main/kotlin/com/shareshelf/auth/entity/User.kt,
    backend/src/main/kotlin/com/shareshelf/auth/entity/UserRepository.kt,
    backend/src/main/kotlin/com/shareshelf/auth/AuthService.kt
  </files>
  <behavior>
    - V13 migration adds google_id VARCHAR(255) UNIQUE, auth_provider VARCHAR(20) NOT NULL DEFAULT 'LOCAL', makes password_hash DROP NOT NULL, creates partial index on google_id
    - User entity gains googleId: String? and authProvider: AuthProvider fields with correct column annotations
    - AuthProvider enum class with LOCAL and GOOGLE values exists in the entity package
    - UserRepository.findByGoogleId(googleId: String): User? method works
    - AuthService.login() throws BadCredentialsException with message "This account uses Google Sign-In. Please use Google to log in." when authProvider is GOOGLE and passwordHash is null or empty
    - Existing LOCAL users with passwordHash set can still log in normally
  </behavior>
  <action>
    1. Create Flyway migration file `V13__add_google_oauth.sql` in `backend/src/main/resources/db/migration/` with three ALTER TABLE statements and one CREATE INDEX:
       - `ALTER TABLE users ADD COLUMN google_id VARCHAR(255) UNIQUE;`
       - `ALTER TABLE users ADD COLUMN auth_provider VARCHAR(20) NOT NULL DEFAULT 'LOCAL';`
       - `ALTER TABLE users ALTER COLUMN password_hash DROP NOT NULL;`
       - `CREATE INDEX idx_users_google_id ON users(google_id) WHERE google_id IS NOT NULL;`

    2. Create `AuthProvider` enum class in `backend/src/main/kotlin/com/shareshelf/auth/entity/AuthProvider.kt`:
       - `enum class AuthProvider { LOCAL, GOOGLE }`

    3. Modify `User.kt` entity:
       - Add `@Column(name = "google_id", unique = true) val googleId: String? = null` field
       - Add `@Column(name = "auth_provider", nullable = false) @Enumerated(EnumType.STRING) var authProvider: AuthProvider = AuthProvider.LOCAL` field
       - Change `passwordHash` column annotation to nullable: `@Column(name = "password_hash")` (remove `nullable = false`)

    4. Add `findByGoogleId(googleId: String): User?` method to `UserRepository.kt` interface

    5. Modify `AuthService.login()` method (per D-08):
       - After finding the user by email and before the password check (line 64), add a guard:
       - `if (user.authProvider == AuthProvider.GOOGLE && user.passwordHash.isNullOrEmpty()) { throw BadCredentialsException("This account uses Google Sign-In. Please use Google to log in.") }`
       - Import `com.shareshelf.auth.entity.AuthProvider`

    6. Run `[BLOCKING]` schema push: `cd backend && ./gradlew flywayMigrate` (or start Spring Boot which auto-applies migrations)
  </action>
  <verify>
    <automated>cd /home/wns/winnaingsoe6666/ShareShelf/backend && ./gradlew flywayMigrate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/main/resources/db/migration/V13__add_google_oauth.sql` exists and contains all 4 SQL statements
    - `User.kt` has `googleId` and `authProvider` fields with correct JPA annotations
    - `passwordHash` column annotation no longer has `nullable = false`
    - `UserRepository.kt` has `findByGoogleId` method
    - `AuthService.login()` checks `user.authProvider == AuthProvider.GOOGLE` before password verification
    - `AuthProvider.kt` enum file exists in auth/entity package
    - Flyway migration applies cleanly (or Spring Boot starts without schema validation errors)
    - `cd backend && ./gradlew compileKotlin` succeeds
  </acceptance_criteria>
  <done>V13 migration adds google_id, auth_provider columns; password_hash is nullable; User entity has new fields; UserRepository has findByGoogleId; AuthService rejects password login for Google-only users</done>
</task>

</tasks>

<verification>
1. `cd backend && ./gradlew flywayMigrate` — migration V13 applies cleanly
2. `cd backend && ./gradlew compileKotlin` — all Kotlin sources compile
3. `grep -r "googleId" backend/src/main/kotlin/com/shareshelf/auth/` — entity and repository references present
4. `grep -r "AuthProvider" backend/src/main/kotlin/com/shareshelf/auth/` — enum used in entity and service
5. `grep "authProvider.*GOOGLE" backend/src/main/kotlin/com/shareshelf/auth/AuthService.kt` — login guard present
</verification>

<success_criteria>
- V13 Flyway migration creates google_id and auth_provider columns with correct types and defaults
- password_hash column is nullable in both migration and entity
- User entity compiles with new fields and correct annotations
- UserRepository.findByGoogleId method exists and returns User?
- AuthService.login() throws specific error for Google-only users attempting password login
- Existing users (auth_provider = 'LOCAL') are unaffected
</success_criteria>

<output>
Create `.planning/phases/07-google-oauth-signup/07-01-SUMMARY.md` when done
</output>
