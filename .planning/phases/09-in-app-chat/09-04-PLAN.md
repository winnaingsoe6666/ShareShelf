---
phase: 09-in-app-chat
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03a
  - 09-03b
  - 09-03c
files_modified:
  - backend/src/test/kotlin/com/shareshelf/chat/ChatServiceTest.kt
  - backend/src/test/kotlin/com/shareshelf/chat/ChatControllerTest.kt
  - frontend/src/components/chat/__tests__/ChatWindow.test.tsx
  - frontend/src/components/chat/__tests__/ConversationList.test.tsx
  - frontend/src/lib/__tests__/chat.test.ts
autonomous: true
requirements:
  - CHAT-04
must_haves:
  truths:
    - "ChatServiceTest covers sendMessage, getConversation, getConversations, markAsRead, getUnreadCount"
    - "ChatControllerTest covers REST endpoints and STOMP messaging"
    - "Frontend tests cover ChatWindow, ConversationList, and chat lib"
    - "All backend tests pass"
    - "All frontend tests pass"
  artifacts:
    - path: "backend/src/test/kotlin/com/shareshelf/chat/ChatServiceTest.kt"
      provides: "ChatService unit tests"
      contains: "ChatServiceTest"
    - path: "backend/src/test/kotlin/com/shareshelf/chat/ChatControllerTest.kt"
      provides: "ChatController unit tests"
      contains: "ChatControllerTest"
    - path: "frontend/src/components/chat/__tests__/ChatWindow.test.tsx"
      provides: "ChatWindow component tests"
      contains: "ChatWindow"
    - path: "frontend/src/components/chat/__tests__/ConversationList.test.tsx"
      provides: "ConversationList component tests"
      contains: "ConversationList"
    - path: "frontend/src/lib/__tests__/chat.test.ts"
      provides: "Chat API lib tests"
      contains: "chat"
---

<objective>
Add comprehensive tests for the chat feature: backend JUnit 5 + MockK tests for ChatService and ChatController, frontend Vitest + RTL tests for ChatWindow, ConversationList, and chat API lib.

Purpose: Ensures chat functionality works correctly and prevents regressions. Tests cover the core business logic (access gating, message persistence, mark-as-read) and UI rendering (bubbles, conversation list, unread badges).

Output:
- ChatServiceTest.kt — unit tests for ChatService methods
- ChatControllerTest.kt — unit tests for REST and STOMP endpoints
- ChatWindow.test.tsx — component tests for chat window
- ConversationList.test.tsx — component tests for conversation list
- chat.test.ts — tests for chat API functions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-in-app-chat/09-CONTEXT.md
@.planning/phases/09-in-app-chat/09-RESEARCH.md
@.planning/phases/09-in-app-chat/09-02-PLAN.md
@.planning/phases/09-in-app-chat/09-03a-PLAN.md
@.planning/phases/09-in-app-chat/09-03b-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatServiceTest — unit tests for ChatService</name>
  <files>
    backend/src/test/kotlin/com/shareshelf/chat/ChatServiceTest.kt
  </files>
  <read_first>
    - backend/src/main/kotlin/com/shareshelf/chat/ChatService.kt (methods to test: sendMessage, getConversation, getConversations, markAsRead, getUnreadCount)
    - backend/src/main/kotlin/com/shareshelf/chat/entity/ChatMessage.kt (entity fields)
    - backend/src/main/kotlin/com/shareshelf/chat/ChatRepository.kt (repository interface)
    - backend/src/main/kotlin/com/shareshelf/borrow/BorrowRepository.kt (used for access check)
    - backend/src/test/kotlin/com/shareshelf/item/ItemServiceTest.kt (existing test pattern: @ExtendWith(MockKExtension::class), @MockK, @InjectImplKs)
    - backend/src/test/kotlin/com/shareshelf/borrow/BorrowServiceTest.kt (existing test pattern)
  </read_first>
  <action>
    Create **backend/src/test/kotlin/com/shareshelf/chat/ChatServiceTest.kt**.

    Follow existing test pattern from ItemServiceTest:
    - @ExtendWith(MockKExtension::class)
    - @MockK lateinit var chatRepository: ChatRepository
    - @MockK lateinit var borrowRepository: BorrowRepository
    - @MockK lateinit var userRepository: UserRepository
    - @MockK lateinit var itemRepository: ItemRepository
    - @InjectImplKs lateinit var chatService: ChatService

    Test cases:

    **sendMessage tests:**
    - "sendMessage succeeds when borrower has borrow request for item" — mock borrowRepository.findByItemIdAndBorrowerIdAndStatus returns non-empty, verify chatRepository.save called
    - "sendMessage succeeds when sender is item owner" — mock item.ownerId == senderId, verify save
    - "sendMessage throws AccessDeniedException when user has no borrow request and is not owner" — mock empty borrow list, mock item.ownerId != senderId
    - "sendMessage throws IllegalArgumentException when sender == receiver" — pass same IDs
    - "sendMessage throws EntityNotFoundException when item not found" — mock itemRepository.findById returns empty
    - "sendMessage caps message at 2000 characters" — verify validation (if enforced in service)

    **getConversation tests:**
    - "getConversation returns messages for valid participant" — mock chatRepository.findConversation returns list
    - "getConversation returns empty list when no messages" — mock returns empty

    **getConversations tests:**
    - "getConversations returns sorted conversation list" — mock repository returns grouped results

    **markAsRead tests:**
    - "markAsRead calls repository with correct params" — verify chatRepository.markAsRead(itemId, senderId, receiverId)

    **getUnreadCount tests:**
    - "getUnreadCount returns count of conversations with unread" — mock repository returns count

    Use every { ... } returns and verify { ... } from MockK.
  </action>
  <verify>
    <automated>
      cd /home/wns/winnaingsoe6666/ShareShelf/backend && ./gradlew test --tests "com.shareshelf.chat.ChatServiceTest" 2>&1 | tail -10
    </automated>
    All ChatServiceTest tests pass.
  </verify>
  <acceptance_criteria>
    - ChatServiceTest uses @ExtendWith(MockKExtension::class) and @MockK annotations
    - ChatServiceTest has at least 5 test methods covering sendMessage, getConversation, getConversations, markAsRead, getUnreadCount
    - sendMessage access gating tested: allowed for borrower with request, allowed for owner, denied for others
    - sendMessage validation tested: sender != receiver, item exists, user exists
    - markAsRead delegates to repository with correct parameters
    - getUnreadCount returns conversation count (not message count)
    - ./gradlew test --tests "com.shareshelf.chat.ChatServiceTest" exits with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>
    ChatServiceTest covers all ChatService methods with proper mocking and assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatControllerTest — unit tests for REST and STOMP controllers</name>
  <files>
    backend/src/test/kotlin/com/shareshelf/chat/ChatControllerTest.kt
  </files>
  <read_first>
    - backend/src/main/kotlin/com/shareshelf/chat/ChatController.kt (REST endpoints to test)
    - backend/src/main/kotlin/com/shareshelf/chat/ChatStompController.kt (STOMP handler to test)
    - backend/src/main/kotlin/com/shareshelf/chat/ChatService.kt (service methods to mock)
    - backend/src/test/kotlin/com/shareshelf/item/ItemControllerTest.kt (existing controller test pattern: @WebMvcTest, MockMvc, @MockkBean)
  </read_first>
  <action>
    Create **backend/src/test/kotlin/com/shareshelf/chat/ChatControllerTest.kt**.

    Follow existing pattern from ItemControllerTest:
    - @WebMvcTest(ChatController::class)
    - @MockkBean lateinit var chatService: ChatService
    - @Autowired lateinit var mockMvc: MockMvc
    - Use @WithMockUser for authenticated requests

    **ChatController REST tests:**

    - "GET /api/chat/conversations returns conversation list" — mock chatService.getConversations, verify 200 + JSON
    - "GET /api/chat/conversations/{itemId}/{otherUserId} returns conversation detail" — mock getConversation, verify 200
    - "GET /api/chat/conversations/{itemId}/{otherUserId} with page/size params" — verify params passed to service
    - "POST /api/chat/conversations/{itemId}/{otherUserId}/read marks messages as read" — mock markAsRead, verify 200
    - "GET /api/chat/unread-count returns unread count" — mock getUnreadCount, verify 200

    **ChatStompController tests (if testable without full WebSocket setup):**
    - Test that sendMessage method calls chatService.sendMessage and sends to SimpMessagingTemplate
    - May need to test at integration level or skip if too complex for unit test

    Use mockMvc.perform(get/post(...)) and andExpect(status().isOk)
  </action>
  <verify>
    <automated>
      cd /home/wns/winnaingsoe6666/ShareShelf/backend && ./gradlew test --tests "com.shareshelf.chat.ChatControllerTest" 2>&1 | tail -10
    </automated>
    All ChatControllerTest tests pass.
  </verify>
  <acceptance_criteria>
    - ChatControllerTest uses @WebMvcTest(ChatController::class) and @MockkBean
    - ChatControllerTest tests all 4 REST endpoints: GET /conversations, GET /conversations/{itemId}/{otherUserId}, POST /conversations/{itemId}/{otherUserId}/read, GET /unread-count
    - Tests verify HTTP 200 status and JSON response structure
    - Tests use @WithMockUser for authentication
    - ./gradlew test --tests "com.shareshelf.chat.ChatControllerTest" exits with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>
    ChatControllerTest covers all REST endpoints with mocked service layer.
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend tests — ChatWindow, ConversationList, chat lib</name>
  <files>
    frontend/src/components/chat/__tests__/ChatWindow.test.tsx,
    frontend/src/components/chat/__tests__/ConversationList.test.tsx,
    frontend/src/lib/__tests__/chat.test.ts
  </files>
  <read_first>
    - frontend/src/components/chat/ChatWindow.tsx (component to test)
    - frontend/src/components/chat/ConversationList.tsx (component to test)
    - frontend/src/lib/chat.ts (functions to test)
    - frontend/src/lib/__tests__/ (existing test patterns — check for vitest.config, test setup)
    - frontend/vitest.config.ts or frontend/vite.config.ts (test configuration)
  </read_first>
  <action>
    Create 3 frontend test files using Vitest + React Testing Library.

    **File 1: frontend/src/lib/__tests__/chat.test.ts**
    - Mock the api module (vi.mock("../../lib/api"))
    - Test getConversations: mock api.get returns data, verify correct URL called
    - Test getConversation: verify URL with itemId, otherUserId, page, size params
    - Test markAsRead: verify POST to correct URL
    - Test getUnreadCount: verify GET /chat/unread-count

    **File 2: frontend/src/components/chat/__tests__/ChatWindow.test.tsx**
    - Mock chat.ts functions (vi.mock("../../lib/chat"))
    - Mock useChatSocket (vi.mock("../../lib/useChatSocket"))
    - Test: renders messages from getConversation
    - Test: shows item title in header
    - Test: send button calls sendMessage
    - Test: textarea has maxlength of 2000
    - Test: calls markAsRead on mount
    - Use render, screen, fireEvent from @testing-library/react

    **File 3: frontend/src/components/chat/__tests__/ConversationList.test.tsx**
    - Mock chat.ts functions
    - Test: renders conversation list items
    - Test: shows unread badge when unreadCount > 0
    - Test: shows empty state when no conversations
    - Test: calls onSelect when row clicked
    - Use render, screen, fireEvent from @testing-library/react

    Follow existing test patterns in the project (check for setupTests.ts, jest-dom matchers).
  </action>
  <verify>
    <automated>
      cd /home/wns/winnaingsoe6666/ShareShelf/frontend && npx vitest run --reporter=verbose 2>&1 | tail -15
    </automated>
    All frontend tests pass.
  </verify>
  <acceptance_criteria>
    - chat.test.ts tests all 4 API functions (getConversations, getConversation, markAsRead, getUnreadCount)
    - chat.test.ts mocks the api module correctly
    - ChatWindow.test.tsx renders messages and shows item title
    - ChatWindow.test.tsx tests send functionality
    - ChatWindow.test.tsx verifies markAsRead called on mount
    - ConversationList.test.tsx renders conversation items
    - ConversationList.test.tsx shows unread badges for unread conversations
    - ConversationList.test.tsx shows empty state
    - ConversationList.test.tsx calls onSelect on row click
    - All tests use Vitest + React Testing Library
    - npx vitest run exits with all tests passing
  </acceptance_criteria>
  <done>
    Frontend tests cover chat API functions, ChatWindow component, and ConversationList component.
  </done>
</task>

</tasks>

<verification>
After this plan completes, verify:
1. ./gradlew test --tests "com.shareshelf.chat.*" passes all backend chat tests
2. npx vitest run passes all frontend chat tests
3. Test coverage includes: access gating, message persistence, mark-as-read, component rendering
</verification>

<success_criteria>
- ChatServiceTest covers sendMessage (with access gating), getConversation, getConversations, markAsRead, getUnreadCount
- ChatControllerTest covers all 4 REST endpoints
- chat.test.ts covers all 4 API functions
- ChatWindow.test.tsx renders messages and handles send
- ConversationList.test.tsx renders conversations with unread badges
- All backend tests pass: ./gradlew test
- All frontend tests pass: npx vitest run
</success_criteria>

<output>
Create `.planning/phases/09-in-app-chat/09-04-SUMMARY.md` when done
</output>
